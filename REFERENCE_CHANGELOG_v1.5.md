# Massage Flow — Changelog

## v1.5 — 2026-08-17

本版把 **程序預覽受控編輯** 接入可測試 Prototype。用戶可重排概括部位優先次序、以 30 秒為單位調整主要段落，並在自己按的替代段落改用較柔和版本。每次調整均由規則引擎重新計算，保留總時長、暖身與收尾；示範與播放器讀取同一份更新結果。

| 範疇 | v1.5 更新 |
|---|---|
| 編輯意圖 | 新增 `ProgramEditIntent`，只接受目標排序、主要段落時長與已審核替代變體。 |
| 規則 | 過量或不安全的時長要求不會破壞總時間；必要準備／收尾不可移除。 |
| UI | 預覽加入「調整程序」入口；調整後在預覽顯示已套用與重新驗證提示。 |
| 一致性 | 同一份重算 `program` 同時驅動預覽、示範與逐段倒數。 |
| QA | 新增受控重排、時長 override、較柔和替代、總時長與必要階段 invariant 測試。 |

## v1.4 — 2026-08-17

本版把 **感受、程度、持續時間及目標** 接入可測試 Prototype。用戶在選完概括部位及左右側後，先以固定廣東話選項輸入今次情況，再揀總時長。規則引擎會按目標調整暖身／收尾比例；程度 4／5 或以上會使用較保守安排。時長頁及預覽均顯示可解釋原因。

| 範疇 | v1.4 更新 |
|---|---|
| 輸入 | 加入感受、程度、持續時間、目標的 session-level 固定選項。 |
| 編排 | 目標改變暖身／收尾預算；高程度增加兩端保守 buffer，主要時間繼續按部位優先次序分配。 |
| 預覽 | 顯示 `contextNotice` 與 `allocationNotice`，解釋今次選擇如何影響流程。 |
| QA | 新增睡前高程度、活動前及多部位 context 的決定性規則測試。 |

## v1.3 — 2026-08-17

本版把 Prototype 由固定肩／上背入口擴展為 **概括部位選擇 → 優先排序 → 逐一選左右側 → 多部位編排**。首批可測試部位為肩／上背、中背及下背；自己按在每個部位使用可觸及或明示替代動作，幫人按則使用相對應的完整已審核流程。規則引擎保留一次暖身及收尾，並把主要時間優先分配予較早排序的部位。

| 範疇 | v1.3 更新 |
|---|---|
| 選區 | 加入上背、中背、下背多選、左右側與無障礙上／下優先排序。 |
| 編排 | `RegionTarget[]` 按陣列次序分配主要時長；所有 segment 保留來源 region。 |
| 模式差異 | 三類部位均支援自己按替代與幫人按完整動作集合。 |
| QA | 新增中背／下背替代及多部位時間優先分配 golden fixture。 |

## v1.2 — 2026-08-17

本版加入 **自己按／幫他人按的模式可觸及性規則**。自己按唔再只係另一套文案或鏡像視角：系統要在選區及規則生成前排除難以自行接觸的動作，並只可使用已審核替代；幫他人按則可保留完整已審核肩背流程。

| 範疇 | v1.2 更新 |
|---|---|
| 產品規則 | 定義 `DIRECT`、`SUBSTITUTE_ONLY`、`UNAVAILABLE` 三種可觸及性結果；自己按不可使用 `UNAVAILABLE`。 |
| UI Flow | 自己按在選區顯示可觸及說明與替代提示；預覽、示範與播放器一致標示替代 segment。 |
| 技術與契約 | 規則引擎加入 `regionReachabilityMap`、action `self_reachability` 及 snapshot `modeAdaptation`。 |
| QA | 新增自己按不可生成難觸及動作、替代原因保存、兩種模式輸出差異與預覽／播放器一致性測試。 |
| Prototype | 肩／上背自己按會省略中央上背／菱形肌步驟，改用「肩胛周邊替代放鬆」；幫他人按保留完整五段流程。 |

> 這次更新的原則是：模式差異必須由選區、規則、預覽及播放器共同執行，不能只在畫面文案中描述。

## v1.1 — 2026-08-17

本版把使用者確認的 **「智能流程編排 v1」** 納入 MVP。核心改動是保留概括部位選擇，例如肩頸、上背、中背、下背與小腿；系統則在選擇時長後，以已審核規則自動安排細分肌群、手法、力度、姿勢、時段及示範資產。此改動不包含任何應用程式程式碼實作。

| 範疇 | v1.1 更新 |
|---|---|
| 產品流程 | 改為「概括選區 → 時長與偏好 → 智能流程編排 → 程序預覽／示範 → 逐段倒數執行」。 |
| MVP 路線 | 不新增產品版本或獨立 roadmap phase；在 MVP Foundation 內採用 MVP-A、MVP-B、MVP-C 的交付順序。 |
| MVP-A | 建立由概括區域映射到細分肌群、手法、姿勢及時長的決定性編排規則與 `Program Segment` 快照。 |
| MVP-B | 提供程序時間軸、細分安排、縮圖／3D 示範入口及受控編輯。 |
| MVP-C | 由同一份確認後的 `Program Segment` 快照驅動 3D、語音、階段倒數、下一步提示及完成事件。 |
| 資料契約 | Plan input、plan snapshot 及 Unity bridge 升至 v2；輸入移除 MVP 手動肌肉點選，輸出保留系統已細分的肌群與示範資產。 |
| QA | 新增概括選區、自動肌群細分、預覽與播放器一致性、示範、逐段自動推進的 fixture、invariant 和測試案例。 |

| 已更新文件 | 主要更新 |
|---|---|
| `01_product_spec.md` | 功能定位、端到端流程、MVP 範圍、驗收、路線圖及 MVP-A／B／C。 |
| `02_technical_spec.md` | Orchestration 架構、資料模型、bridge、規則引擎、實作切片、ADR。 |
| `03_project_overview.md` | MVP 定義、開工次序、首批工作項目及產品路線。 |
| `supporting/04_ui_flow_reference.md` | 概括選區、程序預覽／示範及逐段播放器畫面。 |
| `supporting/05_test_plan.md` | 編排、示範、預覽與執行一致性的測試範圍。 |
| `supporting/07_api_data_contracts.md` | v2 plan／snapshot／bridge contract、`Program Segment` 與分析事件。 |
| `decision_log.md` | 新增第七階段決定，保留此次產品取捨與原因。 |
| `README.md` | 更新文件包狀態與 MVP 內部開發順序。 |

> 本次更新的核心原則是：用戶不需要先理解或手動選擇肌肉；系統應在倒數前清楚展示和示範它如何細分安排，而倒數頁則專責即時帶領執行。

## 相容性與後續工作

v1.1 是規格文件更新，而非已實作功能。實作時需先建立 v2 JSON Schema／bridge schema 的向後相容策略，並以 golden fixtures 驗證同一輸入在本機及日後雲端規則引擎中得到一致的 `Program Segment` 快照。
