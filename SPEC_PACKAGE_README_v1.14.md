# Massage Flow — Specification Package v1.14

This package carries the v1.5 base specification and approved addenda through v1.14. `WEB_3D_PREVIEW_FIX_v1.14.md` records the correction that enables Three.js rendering in the mobile web preview while retaining the vector map only as a WebGL-unavailable fallback.

The runnable portable baseline is `Massage_Flow_Prototype_Source_v1.9.zip`.
